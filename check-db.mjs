import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('posts').select('slug, content, featured_image').eq('slug', 'bim-obligatorio-peru-2026').then(res => { console.log('Image:', res.data[0].featured_image); console.log('Content match:', res.data[0].content.includes('bim-obligatorio-peru.svg')); });
